import styled from 'styled-components';

/**
 * Flex Grid UI Component, for flexbox structure
 */
const Grid = styled.div`
  display: flex;
  flex: 1;
  flex-direction: column;
`;

export default Grid;
